"""
hosts is a list of (IP address, port1, port2)

ordering can be either 'casual' or 'total'
"""

config = {
    'hosts': [
        ('localhost', 16400, 16500),
        ('localhost', 16401, 16501),
        ('localhost', 16402, 16502),
        ('localhost', 16403, 16503),
    ],
}
